# 2026-03-31 日记

## MiniMax 技能全家桶安装完成

### 文档类（从本地市场安装）
- `minimax-docx` — Word 文档
- `minimax-pdf` — PDF 文档
- `minimax-xlsx` — Excel 表格

### 生图/生视频（自建）
- `minimax-media` — 路径 `~/.workbuddy/skills/minimax-media/`
  - `scripts/image_gen.py` — 文生图 + 图生图
  - `scripts/video_gen.py` — 文生视频 + 图生视频 + 首尾帧
- API Key 已写入 `~/.zshrc`，测试验证有效 ✅
- API 返回 `image_base64` 为单元素列表，需 `ib[0]` 取值（已修复脚本兼容）

### 其他
- Python 3.9 不支持 `str | None` 语法，需用 `Optional[str]`（已修复）
